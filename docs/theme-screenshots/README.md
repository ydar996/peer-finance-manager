# Plantvest theme screenshots

Captured locally on 2026-08-24 from `http://localhost:3457`.

## Sign-in and register

| File | Viewport | Page |
|------|----------|------|
| `admin-signin-1280.png` | 1280px | `/admin` |
| `admin-signin-768.png` | 768px | `/admin` |
| `admin-signin-360.png` | 360px | `/admin` |
| `member-signin-1280.png` | 1280px | `/member` |
| `register-1280.png` | 1280px | `/register` |

`/staff` and `/platform` share the same two-column sign-in layout as `/admin` and `/member` (title and fields differ).

## Authenticated workspace (after Import/Users restore)

| File | Viewport | View |
|------|----------|------|
| `import-1280.png` | 1280px | Import (sections restored) |
| `users-1280.png` | 1280px | Users (Sign-In Pages, provision, Create Account) |
| `statements-1280.png` | 1280px | Statements (workbook rows, Choose File). Header uses a 40-character display name with ellipsis; sign-out stays visible |
| `meetings-1280.png` | 1280px | Meetings (52px collapsible rows) |
| `meetings-390.png` | 390px | Meetings |

Public About/Bylaws live at `/c/<slug>/about` and `/c/<slug>/bylaws`. Marketing `/product` and `/brochure` pages are unchanged.
